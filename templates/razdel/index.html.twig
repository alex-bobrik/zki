{% extends 'student_base.html.twig' %}

{% block title %}Hello RazdelController!{% endblock %}

{% block body %}

    <style>
        .search {
            display: flex;
        }

        .search-input {
            width: 100%;
        }
    </style>

    <div class="container" style="margin-top: 20px">
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('admin_razdel_new') }}">Новый раздел</a>
            <a href="{{ path('admin_lection_new') }}">Новая лекция</a>
            <hr>
        {% endif %}

        {{ form_start(formSearch) }}
        <div class="search">
            <div class="search-input">
                {{ form_widget(formSearch.query, {
                    'attr': {
                        'placeholder': 'Поиск по лекциям',
                    }
                }) }}
            </div>
            <div class="search-submit">
                {{ form_widget(formSearch.submit) }}
            </div>
            <div class="clear-search">
                <a href="{{ path('razdel') }}" class="btn btn-outline-dark">Очистить</a>
            </div>
        </div>
        {{ form_end(formSearch) }}


        {% if foundLections%}
            <h2>Найденные лекции:</h2>
            {% for lection in foundLections %}
                <p>
                    <a href="{{ path('razdel_lection_info', {'id': lection.razdel.id, 'lectId': lection.id}) }}">
                        {{ lection.name }}
                    </a>
                </p>
            {% endfor %}
        {% else %}
            <h2>Разделы:</h2>
            {% for razdel in razdels %}
                <p>
                    <a href="{{ path('razdel_lections', {'id': razdel.id}) }}">{{ razdel.name }}</a>
                </p>
            {% endfor %}
        {% endif %}
    </div>



{% endblock %}
